#
# WARP
# Neovim Ansible Role default vars
#

nvim:
  version: 0.4.3-3
  plugins:
    vim_plug: 0.11.0
    nodejs:
  tooling:
    snap:
      universal-ctags: latest/stable
    apt:
      fzf: 0.20.0-1
